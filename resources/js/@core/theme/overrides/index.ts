import button from './button';
import input from './input';
import list from './list';
import menu from './menu';

const overrides = Object.assign({}, button, input, list, menu);

export default overrides;
